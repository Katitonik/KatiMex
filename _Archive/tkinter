import tkinter as tk
from tkinter import ttk
window = tk.Tk()
window.title("Combobox")
window.geometry('500x250')
ttk.Label(window, text = "El Fuego Cocoracha").grid(row = 0, column = 1)
ttk.Label(window, text = "Menu : ").grid(column = 0, row = 5)
n = tk.StringVar()
menuchosen = ttk.Combobox(window, width = 27, textvariable = n)
menuchosen['values'] = ('Mole', 'Nachos', 'Enchiladas')
menuchosen.grid(column = 1, row = 5)
menuchosen.current()
dir(Tk)
dir(Combobox)
dir(menuchosen)
dir(grid)
%history -f tkinter
